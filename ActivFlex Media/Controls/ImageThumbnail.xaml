<UserControl x:Class="ActivFlex.Controls.ImageThumbnail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             x:Name="imgThumbnail"
             d:DesignHeight="300" d:DesignWidth="300"
             Foreground="{StaticResource Thumbnail.Text.Color}">

    <Grid Style="{StaticResource TextThumbnailStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Button x:Name="btnThumbnail" Grid.Row="0" Click="Thumbnail_Click" MouseDoubleClick="Thumbnail_DoubleClick"/>
        <Rectangle x:Name="nameBackground" Grid.Row="1" Height="25" Width="{Binding Path=ActualWidth, ElementName=nameDisplay}" RadiusX="7" RadiusY="7"/>
        <Label x:Name="nameDisplay" Content="{Binding Text, ElementName=imgThumbnail}" HorizontalAlignment="Center" Grid.Row="1" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
    </Grid>
</UserControl>
